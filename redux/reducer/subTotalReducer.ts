import * as types from '../actions/types';

// INITIAL STATE USER 
export const initialStoreState = {
   subTotal: {}
}

// SUBTOTAL REDUCER
export const subTotalReducer = (state = initialStoreState, { type, subTotal }) => {
  switch (type) {
    default:
      return state
  }
}