import * as types from '../actions/types';

// INITIAL STATE USER 
export const initialStoreState = {
   deliveryType: {}
}

// DELIVERY TYPE REDUCER
export const deliveryTypeReducer = (state = initialStoreState, { type, deliveryType }) => {
  switch (type) {
    default:
      return state
  }
}